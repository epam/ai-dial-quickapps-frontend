# See README.md for full documentation of all variables.

# --- Server ---
PORT=

# --- Authentication (NextAuth + Keycloak) ---
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=
AUTH_KEYCLOAK_ISSUER=
AUTH_KEYCLOAK_CLIENT_ID=
AUTH_KEYCLOAK_CLIENT_SECRET=

# --- DIAL core ---
DIAL_CORE_URL=

# --- Themes ---
# Must point directly to config.json, not the base URL.
THEMES_URL=

# --- Client-side ---
# Set to the exact ai-dial-chat URL in production; * is unsafe outside local dev.
NEXT_PUBLIC_ALLOWED_ORIGIN=*
NEXT_PUBLIC_QUICK_APPS_DEFAULT_MODEL=gpt-4o
# Override only if your DIAL instance uses a non-standard schema registry URL.
NEXT_PUBLIC_QUICK_APPS_SCHEMA_2_ID=https://mydial.epam.com/custom_application_schemas/quickapps2
